# 2026-04-09

## MacBook Codex Week 1 validation

- 按主线执行 `make setup -> make doctor -> make start`，`127.0.0.1:8001` 上 `/health`、`/docs`、`/panel` 均可用。
- `make setup` 完成，但 venv 内出现 pip resolver warning：`semgrep/jsonschema`、`semgrep/mcp`、`opentelemetry-proto/protobuf` 版本不一致。
- `dashboard/` 可在 `localhost:3000` 启动，但 `dashboard/app/` 只有页面路由，没有 `app/api/*`；README/PROJECT/DELIVERY 仍声称存在 5 个 `/api/*` 端点，实际全部 404。
- OpenClaw 迁移验证需显式覆盖 `BASE_URL=http://127.0.0.1:8001`；`verify-migration.sh` 在该覆盖下通过。
- `migration/openclaw/scripts/discover-openclaw-data.sh` 原先会因缺失旧路径提前退出，已修成继续输出 missing dir 和候选结果。
- 受限 Mac standby worker smoke 已完成：
  - `noop` completed
  - `cleanup_appledouble` dry run completed，`deleted_count=3`
- 本轮结束后已主动停掉本地 API、dashboard dev server 和 Mac worker，未把 MacBook 留成常驻节点。

## Week 2 follow-up

- 新增 `docs/dashboard-gap-checklist-2026-04-09.md`，把 dashboard 当前定位、5 个 `/api/*` 404、文档/代码漂移和非目标写成单独 gap 清单。
- 新增 `scripts/macbook-week2-smoke.sh` 与 `docs/runbooks/macbook-week2-smoke.md`，把 MacBook 受限 smoke 固化成可复跑命令集。
- `scripts/macbook-week2-smoke.sh` 已实跑通过：
  - `noop` completed
  - `cleanup_appledouble` dry run completed
  - 输出明确注明：`result.deleted_count` 在 `dry_run=true` 时应理解为 candidate count，而不是实际删除数
