# 2026-04-03

- 建立了共享 agent contract 与最小 routing foundation：
  - 新增 `src/autoresearch/routing/`，提供 `RoutingInput`、`RoutingDecision`、`RoutingPolicyOverlay`、`RoutingResolver`。
  - 明确 routing 只负责声明式“选谁/怎么配”，不直接执行 agent，不改 fallback，不绕过 runner / validator / decision。
  - `policy_overlay` 仅允许 additive overlay，实际生效仍走 `build_effective_policy(...)` 的 deny-wins 合并。
- 在 `src/autoresearch/agent_protocol/models.py` 补充 ownership 注释，明确 `JobSpec`/`ExecutionPolicy`/`FallbackStep`/`DriverResult`/`RunSummary` 的边界。
- 在 `src/autoresearch/agent_protocol/registry.py` 增加 `load_all()`，并约定忽略 `configs/agents/_shared*` 共享配置文件。
- 新增文档 `docs/agent-contract-and-routing-foundation.md`，包含：
  - routing 决策点
  - policy / fallback / direct trigger 边界
  - 最小时序图
  - owning areas 与 non-goals
- 新增测试 `tests/test_routing_foundation.py`，覆盖：
  - 默认路由
  - 显式 `agent_id` 覆盖
  - 歧义/冲突输入拒绝
  - routing 不得放宽 hard policy
- 后续根据 review 收口了 3 个确认点：
  - `RoutingDecision` contract shape 增加最小字段面测试，锁定不含 fallback / retry / promotion / execution-state 字段。
  - 文档明确 manifest 扫描顺序不是路由优先级，resolver 只按显式规则选路。
  - `apply_policy_overlay()` 改成真正 additive-only：只能收紧现有 job policy，不能覆盖放宽。
- 新增独立的 control-plane request builder：
  - `src/autoresearch/routing/builder.py` 提供 `ControlPlaneJobRequest`、`ControlPlaneJobBuilder`、`ControlPlaneJobBuildResult`。
  - builder 负责 `RoutingResolver.decide(...) -> apply_policy_overlay(...) -> validator profile materialize -> JobSpec`。
  - `scripts/agent_run.py` 已改为通过这个 builder 落 `JobSpec`，runner 主链路保持不变。
- 根据 review 再补了 4 条 builder 护栏到代码/文档/测试：
  - fail-closed：builder 不兜底选路，不按 manifest 扫描顺序挑 agent。
  - policy overlay 只能收紧 request policy，不能放宽工具/网络/路径/limits。
  - builder 不合成 workflow，不生成 retry/fallback 计划。
  - `scripts/agent_run.py` 明确保持为“显式 agent 的兼容壳”，不承载自动路由语义。
