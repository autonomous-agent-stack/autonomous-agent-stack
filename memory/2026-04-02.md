# 2026-04-02

- Telegram 本地自检脚本 `scripts/telegram_self_check.py` 新增 `--offline` 模式。
- 离线模式下：
  - token、allowlist、poller、`getMe`、`send_test` 改为 `SKIP`，不再误报失败。
  - 使用合成 UID（默认 `999999`）直接打本地 `/api/v1/gateway/telegram/webhook`。
  - 本地 webhook 自检改为 `/help` 和 `/status` 两条稳定命令。
- 新增测试 `tests/test_telegram_self_check.py`，覆盖无 token、无 allowlist 的离线自检路径。
- 回归验证通过：
  - `pytest -q tests/test_telegram_self_check.py`
  - `pytest -q tests/test_gateway_telegram.py -k 'routes_to_openclaw_and_agents or webhook_secret_token_guard or webhook_separates_private_and_group_sessions'`

- Telegram 线上排查补充：
  - `agent_7746300d1320` 的 `Execution error` 不是网关/执行器失败；数据库记录显示 `status=completed`、`returncode=0`，只是上游 `claude --print` 自身输出了通用错误文本。
  - `mgrdispatch_85156cb44a49` 才是真失败，原因是 `stalled_no_progress` / `no workspace progress for 105s`，属于 manager/OpenHands 基础设施层卡住。
  - 已调整 Telegram 结果文案：若 completed 但 stdout 是 `Execution error` / `internal error` / `unexpected error` 这类通用失败文本，会追加解释说明；若 prompt 涉及 `commit` / `push` / `提交` / `推送` 等仓库写动作，会提示改用 `/task <需求>`。
  - 回归验证通过：
    - `pytest -q tests/test_gateway_telegram.py -k 'generic_cli_failure_for_repo_write_prompt or routes_to_openclaw_and_agents or task_command or status_command'`

- Added phase-2 README documenting the two intentional-failure probes and their stable outcomes.
- Confirmed fail-business-assertion-mismatch now lands on driver success + business_validation failure.
