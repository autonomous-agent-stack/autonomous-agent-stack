# 2026-04-06

## 14:31-14:39 | Linux 离线，Mac 临时接管执行面 ✅

- 背景：用户要求在 Linux 节点离线期间，由当前 Mac 助手临时接管。
- 启动前确认：
  - 已读取 `SOUL.md`、`USER.md`、`MEMORY.md`
  - `memory/2026-04-06.md` 与 `memory/2026-04-05.md` 当时不存在
  - 本地 `doctor` 结果为 `READY`
- 发现的问题：
  - 在沙箱内直接启动 `uvicorn` 会报 `bind 127.0.0.1:8001 -> operation not permitted`
  - `make start` 默认 `--reload`，不适合这次临时托管排障
- 处理：
  - 改为在沙箱外启动单进程 `uvicorn`
  - 监听地址：`127.0.0.1:8001`
  - 进程 PID：`15032`
- 验证结果：
  - `GET /health` -> `{"status":"ok"}`
  - `GET /api/v1/gateway/telegram/health` -> `{"status":"ok"}`
  - `GET /api/v1/system/health` -> `status=online`
- 当前结论：Mac 执行面已成功临时接管，可继续承担本地 API / 网关类任务。

## 14:40-15:05 | 切换为 Linux 同一 Telegram bot 并修配置收口 ✅

- 用户明确要求：Mac 临时接管时必须使用 Linux 那个 bot，而不是 Mac 另一只 bot。
- 根因确认：
  - 根目录 `.env` 里是另一只 Mac bot
  - `.env.linux` 里才是 Linux bot `@Linux_vm_bot`
  - API 启动链默认不自动加载 `.env.local`
  - poller 默认端口仍有 `8000` 遗留
  - poller 自身文件加载顺序无法让 `.env.local` 覆盖 `.env`
- 已修改：
  - `scripts/dev-start.sh`：启动时加载 `.env` + `.env.local`
  - `migration/openclaw/scripts/start-telegram-poller.sh`：默认 API 端口改为 `8001`
  - `migration/openclaw/scripts/telegram_poller_bridge.py`：
    - 默认 webhook 端口改为 `8001`
    - 修正 env 文件覆盖顺序，保留外部环境优先、允许 `.env.local` 覆盖 `.env`
  - `src/autoresearch/api/settings.py`：
    - `api_port` 默认值改为 `8001`
    - panel 默认 base URL 改为 `127.0.0.1:8001`
  - `.env.local`：新增 Linux bot takeover override
- 运行态结果：
  - API 已在 `127.0.0.1:8001` 运行，PID `35197`
  - 运行态 token 已切为 Linux bot
  - `telegram_getMe` 返回 bot 用户名：`@Linux_vm_bot`
  - 本地 webhook 合成自检通过（`accepted=True`）
- 冲突排查：
- 期间出现 `409 Conflict`
- 原因是当前 Mac 上残留了两个重复的 Linux bot poller，本地自相冲突，不是 Linux 远端抢占
- 已清理重复 poller，仅保留一个 `screen` 托管实例

## 15:06-15:25 | YouTube agent skeleton 入仓 ✅

- 背景：用户确认仓库内原先只有 YouTube 监控/字幕脚本链路，没有独立 agent 实现。
- 结论：按当前 FastAPI + dependency + SQLiteModelRepository 架构，先落一个单服务 YouTube bounded context，不走多 Agent。
- 已新增：
  - `src/autoresearch/api/routers/youtube.py`
  - `src/autoresearch/core/services/youtube_agent.py`
  - `src/autoresearch/core/services/youtube_fetcher.py`
  - `src/autoresearch/core/services/youtube_digest.py`
  - `tests/test_youtube_agent_service.py`
  - `tests/test_youtube_router.py`
- 已补充模型与接线：
  - `src/autoresearch/shared/models.py` 增加 subscription/video/transcript/digest/run/Q&A 模型
  - `src/autoresearch/api/dependencies.py` 增加 `get_youtube_agent_service()`
  - `src/autoresearch/api/main.py` 挂载 YouTube router
- 当前能力：
  - 订阅 YouTube source
  - 检查更新并落视频元数据
  - 拉取字幕接口骨架（默认 fetcher 封装 `yt-dlp`）
  - 生成 deterministic digest
  - 基于 transcript/digest 做基础问答
- 验证：
  - `python -m pytest tests/test_youtube_agent_service.py tests/test_youtube_router.py`
  - 结果：3 passed

## 21:30-23:45 | YouTube live hardening、失败分类与真实联机验证 ✅

- 用户明确收口边界：
  - 先不接 Telegram
  - 先不接 cron
  - 先不做多 agent 协同
  - 先把单体 YouTube bounded context 做到可真实运行、可解释失败、可回归验证
- 已新增/重构：
  - `src/autoresearch/core/repositories/youtube.py`
    - 单独抽出 YouTube repository，避免 service 层反复扫列表做判重
  - `src/autoresearch/core/services/youtube_errors.py`
    - 统一 `YouTubeAgentError` 与 failure kind
  - `scripts/youtube_live_smoke.py`
    - 显式 `--allow-live` 的联机 smoke 入口，不进默认 pytest
- 已落地的失败分类：
  - `unsupported_source_or_parse_failed`
  - `no_new_videos_found`
  - `video_unavailable`
  - `transcript_unavailable`
  - `auto_captions_only`
  - `subtitle_language_mismatch`
  - `yt_dlp_extractor_failure`
  - `network_failure`
  - `timeout_failure`
  - `rate_limited`
  - `duplicate_idempotent_noop`
  - `ask_context_missing`
- 持久化改进：
  - run / transcript / digest 都会落 `result_kind`、`failure_kind`、`reason`
  - `manual_check` 对“重复发现但已存在”明确落 `duplicate_idempotent_noop`
  - digest 幂等从“已有即 NOOP”收紧为“内容仍然最新才 NOOP”，解决 metadata refresh 后摘要陈旧
- 离线回归已扩到 11 条：
  - 覆盖 channel/playlist discovery、transcript success/unavailable、digest 幂等与 metadata 刷新再生成、ask 缺上下文错误、重复 check 不重复产出、URL 规范化与 subscription 去重、router structured error
  - 最新结果：`11 passed`
- 真实联机验证：
  - 目标：
    - channel: `https://www.youtube.com/@GoogleDevelopers/videos`
    - playlist: `https://www.youtube.com/playlist?list=UU_x5XG1OV2P6uZZ5FSM9Ttw`
    - video: `https://www.youtube.com/watch?v=6yjJ7Prt-RI`
  - 最新 v4 报告：
    - `/tmp/youtube-live-smoke-output-v4/live-smoke-20260406T154504Z.json`
  - 关键结果：
    - channel 跑通 create/check/metadata/transcript/digest/ask，发现 3 个新视频
    - playlist 跑通 create/check/metadata/transcript/digest/ask，抓到手工 `zh-CN` 字幕
    - video 目标的 `manual_check` 明确返回 `duplicate_idempotent_noop`
    - 英文视频已优先命中手工字幕 `en-j3PyPqV-e1s`
    - digest 不再保留 `Channel: unknown`
    - ask 的回答与 citations 已对齐
- 已知剩余问题：
  - deterministic digest 仍偏朴素，容易带入讲者自述句
  - live smoke 这轮没有自然覆盖 `rate_limited/network_failure/video_unavailable` 等外部失败，只能确认分类代码与离线覆盖已在
- 已输出 readiness report：
  - `artifacts/youtube/readiness-report-2026-04-06.md`

## 23:50-00:05 | YouTube 订阅管理面最小闭环补齐 ✅

- 用户确认当前边界仍然不扩到 Telegram / cron，只补单体 YouTube bounded context 的订阅管理能力。
- 已补能力：
  - `GET /api/v1/youtube/subscriptions/{id}`
  - `PATCH /api/v1/youtube/subscriptions/{id}`
  - `DELETE /api/v1/youtube/subscriptions/{id}`（软删除）
  - `GET /api/v1/youtube/subscriptions/export`
  - `POST /api/v1/youtube/subscriptions/import`
- 设计收口：
  - `YouTubeSubscriptionStatus` 新增 `deleted`
  - create 遇到同一规范化 URL 且状态为 deleted 时，直接恢复原订阅而不是新建重复记录
  - list / export 默认隐藏 deleted，必要时可 `include_deleted=true`
  - import 返回结构化 action：`created` / `updated` / `restored` / `skipped` / `failed`
  - SQLite YouTube repository 补了 subscription lookup 的 `status` / `updated_at` 列与过滤索引，过滤逻辑不再散在 service
- 回归结果：
  - `python -m pytest tests/test_youtube_agent_service.py tests/test_youtube_router.py`
  - 最新结果：`14 passed`
