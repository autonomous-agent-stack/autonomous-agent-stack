# 2026-04-30

- 管家 RUNNING live 卡：`format_butler_live_status_message` 支持 `telegram_live_card_title`；Mac daemon YouTube 阶段上报写入双语标题，避免误显示 Hermes 文案。
- 分派 metadata：`router._build_butler_worker_contract_from_decision` 将 `detected_task_type` / `butler_task_type` 规范为字符串。
- 文档：`docs/decisions/fast-policy-router-and-slow-orchestration-v1.md`、`telegram-youtube-autoflow-ingress-v1.md` 状态改为实现对齐（中英并列段落）。
- 验证：`pytest --noconftest tests/test_telegram_completion_format.py`、`tests/test_butler_router.py` 通过；`test_gateway_telegram` 单测在本环境导入 `app` 时易长时间阻塞（与本次改动无关），Focused 命令建议保留 `--noconftest`。
