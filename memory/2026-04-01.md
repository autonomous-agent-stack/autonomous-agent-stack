## 2026-04-01

- Next mainline after Linux run lifecycle merge:
  WorkerHeartbeat -> WorkerRegistryService real production wiring.
- Reason to prioritize:
  same unified-contract track, bridge already exists, lower risk than making verdict drive retry/fallback, and continues converging the real Linux path into the unified control plane.
- Repo positioning decision:
  `autonomous-agent-stack` should be narrated as a controlled agent execution control plane, not another personal agent runtime, memory system, training framework, or channel OS.
- Four words to anchor docs and roadmap:
  Control Plane / Worker Fabric / Governance Layer / Migration Hub.
- OpenClaw relation:
  keep the repo framed as an OpenClaw-compatible migration hub with phased migration and rollback, not a full replacement runtime.
- Unified contract progress update:
  `WorkerRegistration` is now wired into `WorkerRegistryService` after `run_record` and `WorkerHeartbeat`.
- Verification:
  `PYTHONPATH=src .venv/bin/pytest -q tests/test_linux_run_lifecycle_integration.py tests/test_worker_registry_heartbeat_integration.py tests/test_worker_registry_registration_integration.py`
  passed with 23 tests green.
- Updated bottleneck:
  the main remaining control-plane gap is no longer registration wiring; it is verdict consumption, dynamic/persistent worker state, and real non-Linux workers.
- Prompt orchestration direction:
  MASFactory-style prompt orchestration is viable only as a planning/orchestration frontend on top of the control plane, not as a replacement for the control plane itself.
- Scope rule:
  keep prompt orchestration limited to goal decomposition, role assignment, candidate plan generation, and validation requirements; execution must still flow through JobSpec, driver adapters, worker registry, gate verdict, and acceptance.
- Project doc added:
  `/docs/project-direction.md` now records the control-plane positioning, borrowed project boundaries, prompt orchestration rules, self-exploration rule, and three-layer memory split.
