## 2026-03-31

- Built the first Linux supervisor MVP on branch `feat/linux-supervisor-mvp` instead of continuing abstract planning.
- New execution-plane pieces added:
  - `src/autoresearch/shared/linux_supervisor_contract.py`
  - `src/autoresearch/core/services/linux_supervisor.py`
  - `scripts/linux_housekeeper_supervisor.py`
  - `scripts/linux_housekeeper.sh`
- The MVP intentionally stays narrow:
  - file task protocol under `.masfactory_runtime/linux-housekeeper/queue/<task_id>/`
  - resident loop / `run-once`
  - per-task `task.json`, `status.json`, `heartbeat.json`, `summary.json`, `artifacts/`
  - global supervisor `status` / `heartbeat`
  - watchdog for both total timeout and no-progress stall
  - orphaned running-task repair into `infra_error`
- Remote Linux dispatch was changed from direct `make agent-run` to queueing via:
  - `bash ./scripts/linux_housekeeper.sh enqueue "<prompt>"`
- Added focused regression coverage:
  - success path writes summary + copied artifacts
  - total timeout => `timed_out`
  - no-progress => `stalled_no_progress`
  - mock fallback classification => `mock_fallback`
  - Linux dispatch script now targets the queue-based supervisor path
- Verification passed:
  - `6 passed` for `tests/test_linux_supervisor.py tests/test_dispatch_to_linux.py`
  - `py_compile` passed for the new supervisor contract/service/CLI
  - CLI smoke on temp runtime root:
    - `status` returns empty queue JSON
    - `run-once` returns `null` cleanly on empty queue
- Still intentionally deferred in this MVP:
  - day/night scheduling
  - nightly exploration automation
  - Telegram-triggered supervisor integration
  - auto draft PR flow
  - video-agent
