# 2026-04-07

## 00:10-00:15 | YouTube hardening 状态复核与 readiness report 纠偏 ✅

- 复核用户给出的“先做 live hardening，不扩 Telegram / cron”方向，结论：方向正确，但仓库当前并不是停留在 skeleton。
- 当前已在仓库中落地：
  - 真实联机 smoke 脚本：`scripts/youtube_live_smoke.py`
  - 专用 YouTube repository：`src/autoresearch/core/repositories/youtube.py`
  - 失败分类与状态持久化：`youtube_errors.py` + agent/fetcher/digest 流程
  - 最小订阅管理面：get/update/delete/import/export
- 当场复跑：
  - `python -m pytest tests/test_youtube_agent_service.py tests/test_youtube_router.py`
  - 结果：`14 passed`
- 纠偏：
  - 更新 `artifacts/youtube/readiness-report-2026-04-06.md`
  - 将旧的 `11 passed` 修正为 `14 passed`
  - 补入订阅管理面已完成，移除“还没有 admin surface”的过期判断
- 当前判断：
  - 现在仍不适合接 Telegram
  - 现在仍不适合接 cron
  - 下一步若继续 YouTube 收口，重点应是更广的真实失败样本和 digest 内容质量，而不是外扩入口

## 00:15-00:40 | YouTube hardening follow-up：failure probes + digest 去噪 ✅

- 用户进一步把范围压到两件事：
  - 扩真实失败采样
  - 收紧 digest 去噪
- 已实现：
  - `YouTubeAgentError` / API detail / run/transcript/digest 状态新增稳定 `failed_stage`
  - `youtube_fetcher.py` 将 `yt-dlp` 分类收口到集中 helper，并支持 `rate_limited` / `network_failure` / `timeout_failure` / `video_unavailable` / `yt_dlp_extractor_failure`
  - `scripts/youtube_live_smoke.py` 新增 failure probe 汇总，不伪造 live 结果，无法观测时明确记 `implemented_not_observed_live`
  - `youtube_digest.py` 增加 deterministic 规则去噪，过滤 sponsor、self-intro、greeting、rhetorical hook、CTA/outro、description 噪音行
- 离线回归：
  - `python -m pytest tests/test_youtube_agent_service.py tests/test_youtube_router.py tests/test_youtube_fetcher.py tests/test_youtube_digest.py`
  - 结果：`22 passed`
- 最新 live smoke：
  - 报告：`/tmp/youtube-hardening-20260407-v3/live-smoke-20260406T163845Z.json`
  - digest 示例：`/tmp/youtube-hardening-20260407-v3/channel-6yjJ7Prt-RI.md`
- 已真实观测到的 failure kinds：
  - `video_unavailable`
  - `yt_dlp_extractor_failure`
  - `timeout_failure`
- 已实现但本轮未真实观测：
  - `network_failure`
  - `rate_limited`
- 当前结论仍然不变：
  - 还不适合接 Telegram
  - 还不适合接 cron

## 00:20-00:30 | Agent 执行策略与 Mac standby v1 决策落盘 ✅

- 背景：用户要求减少 Codex 在本地可逆实现任务上的追问，并为 Mac standby worker 线补一份 repo 内可提交的决策文档。
- 已修改：
  - `AGENTS.md`
    - 新增 `Default Execution Policy`
    - 明确“本地、可逆、可测试任务默认直接做”
    - 将 ask-first 条件收紧到 destructive / external / irreversible / missing credential / product fork / unclear production rollback
    - 调整原有 `When in doubt, ask` 与 `Anything you're uncertain about` 表述，避免与默认执行策略打架
  - `docs/decisions/mac-standby-worker-v1.md`
    - 固定 v1 范围：仅 `workers/register`、`workers/heartbeat`、SQLite registry、focused tests
    - 明确 non-goals：不碰 Telegram、不做多 agent、不切到 TS control plane、不扩新 worker type
- 当前结论：
  - 之后这条线的第一刀应是 Python 主应用里的最小 worker control plane 闭环
  - 不是 Telegram fallback，也不是单独再造一个 Mac 新系统

## 00:30-00:45 | Worker control plane 首刀落地：register + heartbeat ✅

- 范围：
  - `src/autoresearch/api/routers/workers.py`
  - `src/autoresearch/core/services/worker_registry.py`
  - `tests/test_worker_registry.py`
  - `src/autoresearch/shared/models.py`
  - `src/autoresearch/api/dependencies.py`
  - `src/autoresearch/api/main.py`
- 已实现：
  - `POST /api/v1/workers/register`
  - `POST /api/v1/workers/{worker_id}/heartbeat`
  - SQLite-backed `worker_registrations` registry
  - `mac` worker type + `mode=standby` 注册
  - duplicate registration 幂等更新
  - stale worker liveness 判定（service 层）
- 明确未做：
  - 不碰 Telegram
  - 不做 claim / lease / scheduler
  - 不碰 TS `agent-control-plane/`
- 验证：
  - `python -m pytest tests/test_worker_registry.py`
  - 结果：`4 passed`

## 05:20-05:35 | YouTube final evidence pass：network live 观测 + generic framing suppression ✅

- 范围继续锁在 YouTube bounded context，不碰 Telegram / cron / 架构扩张。
- 本轮代码收口：
  - `youtube_fetcher.py` 支持命令级 extra args，供 live smoke 做单次受控 connectivity probe
  - `youtube_errors.py` 补充 `failed to establish a new connection` / `can't assign requested address` 等 transport 错误映射到 `network_failure`
  - `scripts/youtube_live_smoke.py` failure probe 结构新增 `probe_attempted` / `observation_status` / `evidence` / `notes`
  - `youtube_digest.py` 继续压 generic framing，去掉 `In this video, we'll take a tour...`，并把 `So meet/So that's ...` 重写成实体句
- 离线回归：
  - `python -m pytest tests/test_youtube_agent_service.py tests/test_youtube_router.py tests/test_youtube_fetcher.py tests/test_youtube_digest.py tests/test_youtube_live_smoke.py`
  - 结果：`28 passed`
- 最新 live smoke：
  - 报告：`/tmp/youtube-hardening-20260407-v5/live-smoke-20260406T213005Z.json`
  - 已真实观测：
    - `video_unavailable`
    - `yt_dlp_extractor_failure`
    - `network_failure`
    - `timeout_failure`
  - 已尝试但未真实观测：
    - `rate_limited`
- digest 实际产物改进：
  - 去掉了 `In this video, we'll take a tour...`
  - `So that's/So meet "AIventure"...` 已变成 `"AIventure" is ...`
  - 最新 digest 示例：`/tmp/youtube-hardening-20260407-v5/channel-6yjJ7Prt-RI.md`
- 当前判断仍不变：
  - 不适合接 Telegram
  - 不适合接 cron

## 05:40-05:55 | YouTube tail polish：manual-ready / automation-not-ready 定位收口 ✅

- 范围继续冻结在单体 YouTube bounded context，不扩 API，不接 Telegram，不接 cron。
- 新增 deterministic 去噪规则：
  - 过滤 `Or if you prefer...`
  - 过滤 `Explore our solution` / `Inspect the full architecture` / `Quiz your knowledge`
  - 过滤 `We can't wait to see...` 这类低信息 closing bridge
- 离线回归：
  - `python -m pytest tests/test_youtube_agent_service.py tests/test_youtube_router.py tests/test_youtube_fetcher.py tests/test_youtube_digest.py tests/test_youtube_live_smoke.py`
  - 结果：`29 passed`
- 最新 live smoke：
  - 报告：`/tmp/youtube-hardening-20260407-v6/live-smoke-20260406T213827Z.json`
  - 最新 digest：`/tmp/youtube-hardening-20260407-v6/channel-6yjJ7Prt-RI.md`
  - `Or if you prefer, you can use Gemini` 已不再进入 digest 主体
  - `rate_limited` 仍保持 `implemented_not_observed_live`
- 对外判断收口：
  - manual-ready：是
  - automation-not-ready：是

## 00:45-01:05 | Worker control plane 第二刀：claim + queue + lease ✅

- 范围：
  - `src/autoresearch/core/services/worker_scheduler.py`
  - `src/autoresearch/api/routers/workers.py`
  - `src/autoresearch/api/dependencies.py`
  - `src/autoresearch/shared/models.py`
  - `tests/test_worker_scheduler.py`
  - `docs/decisions/mac-standby-worker-claim-slice.md`
- 已实现：
  - `POST /api/v1/workers/{worker_id}/claim`
  - 最小 SQLite-backed `worker_run_queue`
  - 最小 SQLite-backed `worker_leases`
  - 仅支持 `housekeeping` 队列
  - 同 worker 重复 claim 返回同一 active lease
  - active lease 对其他 worker 可见为不可领取
  - 过期 lease 可被重新领取
- 明确未做：
  - 不碰 Telegram
  - 不做真实 job 执行
  - 不做 failback / drainback
  - 不做多 worker 匹配策略
- 验证：
  - `python -m pytest tests/test_worker_registry.py tests/test_worker_scheduler.py`
  - 结果：`9 passed`

## 01:05-01:35 | Mac standby worker v1 端到端闭环落地 ✅

- 范围：
  - `src/autoresearch/api/routers/worker_runs.py`
  - `src/autoresearch/api/routers/workers.py`
  - `src/autoresearch/core/services/worker_scheduler.py`
  - `src/autoresearch/shared/models.py`
  - `src/autoresearch/workers/mac/`
  - `scripts/start-mac-worker.sh`
  - `tests/test_worker_run_reporting.py`
  - `tests/test_mac_worker_daemon.py`
  - `docs/decisions/mac-standby-worker-runtime-v1.md`
  - `docs/runbooks/mac-standby-worker-smoke.md`
- 已实现：
  - `POST /api/v1/worker-runs`
  - `POST /api/v1/workers/{worker_id}/runs/{run_id}/report`
  - run payload / task type / requested_by / progress / result / error 持久化
  - terminal report 回收 active lease
  - `mac` standby daemon 自动 register / heartbeat / claim / execute / report
  - 本地 executor 支持 `noop`、`cleanup_appledouble`、`cleanup_tmp`
  - 启动脚本和手工 smoke runbook
- 明确未做：
  - 不碰 Telegram
  - 不做 failback / drainback
  - 不做多 worker 并发池
  - 不碰 TS `agent-control-plane/`

## 05:54-05:57 | Mac standby worker v1 人工 smoke 完成 ✅

- 目标：
  - 用真实本地进程补一轮 runbook 级 smoke，不再只停留在 targeted tests
- 实际执行：
  - 以本地进程启动 Python/FastAPI 控制面
  - 启动 `scripts/start-mac-worker.sh`
  - 创建专用 smoke 目录：`artifacts/mac-worker-smoke/._smoke.txt`
  - enqueue 一个 `noop`
  - enqueue 一个 `cleanup_appledouble` dry-run，root 指向上述 smoke 目录
- 观测结果：
  - worker 成功 `register`
  - worker 持续 `heartbeat + claim`
  - `cleanup_appledouble` dry-run 先被领取并完成，结果中 `deleted_count=1`
  - `noop` 随后被领取并完成，结果中 `echo=smoke noop`
  - SQLite 中两个 `worker_leases` 均已 `active=False`
  - SQLite 中 worker registration 保持最新 `last_heartbeat_at`
  - dry-run 文件 `artifacts/mac-worker-smoke/._smoke.txt` 仍存在，内容未变
- 额外说明：
  - 本地沙箱不允许直接绑定 / 访问 `127.0.0.1:8001`，所以这轮 smoke 通过授权方式在本机侧完成
  - 当前可把这条线定性为：代码闭环完成，自动化验证通过，人工 smoke 通过；仍未进入长时间运行或 production hardening

## 06:05-06:20 | Standby housekeeper -> YouTube manual bridge 落地 ✅

- 范围继续严格收口：
  - 不接 Telegram
  - 不接 cron
  - 不做自治 YouTube 监控
  - 不把 YouTube 状态复制进 standby
- 已实现：
  - 现有 worker run 路径新增 `task_type=youtube_action`

## 07:10-07:35 | Telegram thin ingress 接到 youtube_autoflow ✅

- 用户把基线固定为：
  - `youtube_autoflow` 继续作为唯一真源
  - Telegram 只做薄入口
  - 不在 Telegram 侧复制 YouTube / GitHub 业务逻辑
- 已实现：
  - `gateway_telegram.py` 新增 YouTube ingress 分支，只在消息满足“恰好 1 条合法 YouTube URL”时受理
  - Telegram 入口直接 enqueue 现有 `task_type=youtube_autoflow`
  - 多链接、伪 YouTube 引用等场景 fail-closed 拒绝
  - accepted / rejected / failed 都会给 Telegram 回即时受理结果
  - OpenClaw session 会记录原消息和 `youtube autoflow queued: <run_id>`，并把 `run_id` / source_url 写入 session metadata，给后续 `/cancel` 或补偿动作留标准位
  - 抽出共享 URL helper，Telegram 与 `standby_youtube_autoflow` 共用同一套 URL 提取逻辑
- 明确保持不变：
  - `youtube_autoflow` 语义不变
  - GitHub publish 仍走 `publish_youtube`
  - 非 YouTube 消息继续走原有 Telegram agent 路径，不把 gateway 收窄成只会处理链接
  - 不做远端真实 PR smoke
- 文档：
  - 新增决策文档 `docs/decisions/telegram-youtube-autoflow-ingress-v1.md`
  - 更新 runbook `docs/runbooks/mac-standby-worker-smoke.md`
- 验证：
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_gateway_telegram.py -q` -> `29 passed`
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_gateway_telegram_guards.py -q` -> `5 passed`
  - `PYTHONPATH=src .venv/bin/python -m pytest tests/test_worker_run_reporting.py tests/test_mac_worker_daemon.py tests/test_standby_youtube_autoflow.py -q` -> `11 passed`
  - `StandbyYouTubeAction` / `StandbyYouTubeActionRequest` / `StandbyYouTubeActionResult` 提供 typed handoff contract
  - 新增 `standby_youtube_bridge.py`，由 standby worker 直接 handoff 到 `YouTubeAgentService`
  - `MacWorkerExecutor` 支持执行 `youtube_action`
  - `MacWorkerDaemon` 在 terminal report 中保留 structured `result + error_kind + failed_stage + reason`
- 入口选择：
  - 沿用 `/api/v1/worker-runs` -> `WorkerSchedulerService.enqueue` -> `MacWorkerDaemon` -> `MacWorkerExecutor`
  - 不新增平行脚本编排，不做 API loopback
- 当前支持的手动 YouTube 动作：
  - `subscribe`
  - `check`
  - `fetch_transcript`
  - `build_digest`
  - `ask`
- 失败传播收口：
  - bridge 级 fail-closed：`invalid_request` / `invalid_action` / `invalid_target`
  - YouTube 域内失败继续透传现有 taxonomy，如 `transcript_unavailable` / `timeout_failure`
- 回归结果：
  - `python -m pytest tests/test_standby_youtube_bridge.py tests/test_mac_worker_daemon.py` -> `9 passed`
  - `python -m pytest tests/test_worker_scheduler.py tests/test_worker_run_reporting.py tests/test_mac_worker_daemon.py tests/test_standby_youtube_bridge.py tests/test_youtube_agent_service.py tests/test_youtube_router.py tests/test_youtube_fetcher.py tests/test_youtube_digest.py tests/test_youtube_live_smoke.py` -> `45 passed`
- 当前判断：
  - standby 现在可以作为手动 YouTube 操作的投递/触发入口
  - 这不改变 YouTube 的 readiness 结论：仍然是 manual-ready / automation-not-ready

## 06:45-07:05 | Internal YouTube autoflow 上线口径收口 ✅

- 新落地能力：
  - `youtube_autoflow` 内部入口
  - `Mac standby worker -> YouTube bounded context -> GitHub publish -> draft PR` 自动链
  - repo routing 走 deterministic + fail-closed
- 代码与验证状态：
  - targeted + broader regression 共覆盖 worker / YouTube / GitHub assistant 相关链路
  - 最新回归结果：`74 passed`
- 对外/对内表述纠偏：
  - 现在可以说：内部受控自动链已 code-complete、test-validated，可在 Python control plane 下做受控内部上线
  - 不能说：聊天里随手丢一个 YouTube 链接给 `mac 备用助理` 就能自动全链路跑完
- 当前准确表述：
  - internal rollout ready
  - chat-entry integration out of scope
  - remote PR smoke out of scope
- 下一步若继续推进，最自然的是把 `youtube_autoflow` 接到真正的聊天入口，但这会进入 Telegram / chat ingress 范围
