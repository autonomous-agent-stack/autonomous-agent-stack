# 2026-04-22

- 新增 `docs/windows-wsl2-hermes-control-plane.md`，把 Windows + WSL2 跑 Hermes 并由 AAS 接管的最佳实践写进项目。
- 结论固定为：Hermes 不走原生 Windows，统一通过 WSL2；AAS 通过 `HermesRuntimeAdapterService`、`configs/runtime_agents/hermes.yaml` 和 `metadata.hermes` 接入，不直接耦合进程层。
- 顺手把 README 中的文档导航补上了这条路径，避免后面重复问一次。
