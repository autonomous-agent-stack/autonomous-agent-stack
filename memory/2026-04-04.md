# 2026-04-04

- `subtitle-offline-dev-kit` 的 `scan_subtitle_anomalies.py` 阈值参数已验证稳定：
  - `pytest -q subtitle-offline-dev-kit/tests/test_scan_subtitle_anomalies.py` 通过
  - `pytest -q subtitle-offline-dev-kit/tests` 结果为 `33 passed in 0.70s`
- 对真实字幕语料做了结构异常阈值 sweep，结论如下：
  - `diaryofaceo` 的 `rapid_repeat` 基本不受 `--rapid-repeat-window-seconds` 影响，`0.005` 到 `0.8` 都维持 `207` 个 `zh` 候选，说明这批重复是 gap-free，相邻重复窗口不是有效杠杆
  - `diaryofaceo` 的 `duplicate_cue` 主要集中在 `en`，`--duplicate-min-text-len 60` 时有 `132` 个候选，提到 `80` 后降到 `25`，`90` 时只剩 `1`
  - `wowinsight` 更适合较宽松的结构审计：`--candidate-issue duplicate_cue --candidate-issue rapid_repeat --duplicate-min-text-len 60` 时约 `3` 个候选；若只看 `duplicate_cue` 且 `--duplicate-min-text-len 70`，约 `1` 个候选
- 已把 `harvest` / `audit` 两档推荐命令和 corpus-specific 建议写入：
  - `subtitle-offline-dev-kit/README.md`
  - `subtitle-offline-dev-kit/docs/mac-subtitle-pipeline.md`
- 当前推荐操作规范：
  - `harvest`：优先 `--language en --candidate-issue duplicate_cue --duplicate-min-text-len 80`
  - `audit`：使用 `--candidate-issue duplicate_cue --candidate-issue rapid_repeat --candidate-issue long_cue --candidate-issue large_gap --duplicate-min-text-len 60`
