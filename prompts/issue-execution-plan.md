# Execution Plan

- Repo: `${repo}`
- Issue: `${issue_number}` - `${issue_title}`
- URL: ${issue_url}
- Type: `${issue_type}`
- Priority: `${priority}`

## Goal
${summary}

## Branch
`${branch_name}`

## Commit Message
`${commit_message}`

## Allowed Paths
${allowed_paths}

## Forbidden Paths
${forbidden_paths}

## Validation Commands
${validator_commands}

## Execution Rules
1. Stay inside `allowed_paths`.
2. Keep the patch as small as possible.
3. Run validation before promotion.
4. Never write to the default branch directly.
